# .github/workflows/ci.yml

name: CI
on:
  pull_request:
    types: [closed]

jobs:
  label:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: echo closed
        run: echo "closingggg"
